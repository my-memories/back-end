#!/usr/bin/env groovy
pipeline {
    agent none
    stages {
        stage('Lint') {
            steps {
                sh "./gradlew ktlintFormat "
            }
        }
        stage('Test') {
            steps {
                sh "./gradlew test"
            }
        }
        stage('Build') {
            steps {
                sh "./gradlew clean build -x test"
            }
        }
    }
}